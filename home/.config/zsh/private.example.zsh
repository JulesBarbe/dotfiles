# private zsh config (not tracked)
#
# copy this file to `~/.config/zsh/private.zsh` and fill in work-only settings.

# examples:
# export work_token="..."
# export work_proxy="http://..."
# alias workvpn="..."

